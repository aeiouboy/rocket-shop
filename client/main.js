UI.registerHelper("money", function(amout){
  return accounting.formatMoney(amout);

});

UI.registerHelper("markdown", function(text){
  var converter = new show.down.Converter;
  return accounting.formatMoney(amout);

});
